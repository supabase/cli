version: "2"
linters:
  enable:
    - dogsled
    - dupl
    - gosec
    - misspell
    - nakedret
    - staticcheck
    - unconvert
    - unparam
    - whitespace
  settings:
    dupl:
      threshold: 250
  exclusions:
    generated: lax
    rules:
      - text: 'ST1003:'
        linters:
          - staticcheck
      - text: 'G101:'
        linters:
          - gosec
      - text: 'G115:'
        linters:
          - gosec
      - text: 'G117:'
        linters:
          - gosec
      - text: 'G704:'
        linters:
          - gosec
      - text: 'G705:'
        linters:
          - gosec
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
