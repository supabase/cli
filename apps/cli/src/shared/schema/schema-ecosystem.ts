export const SCHEMA_PULL_NO_MERGE_HELP = `supabase/schemas already has SQL. Pull does not merge files.
  --output <directory>  Write a side-by-side copy
  --force               Replace managed files (_custom/ is left alone)`;
